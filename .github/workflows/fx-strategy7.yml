name: fx-strategy7

on:
  workflow_dispatch:   # manual only - research (news-reaction, spike proxy)

permissions:
  contents: write

concurrency:
  group: fx-strategy7
  cancel-in-progress: false

jobs:
  strategy7:
    runs-on: ubuntu-latest
    timeout-minutes: 120        # M15 x 7 pairs over ~8 years = heavier fetch
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Integrity check (guards against corrupted files)
        run: python -m compileall -q src tests

      - name: Unit tests
        run: |
          python tests/test_prevday_fx.py
          python tests/test_strategy7.py

      - name: Run news-reaction research
        env:
          OANDA_API_KEY: ${{ secrets.OANDA_API_KEY }}
          OANDA_ENV: practice
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        run: python -m src.backtest.strategy7_news_reaction

      - name: Commit reports
        if: always()
        run: |
          git config user.name "fx-research-bot"
          git config user.email "fx-research-bot@users.noreply.github.com"
          git add reports/
          git diff --cached --quiet || git commit -m "fx strategy7 news-reaction research $(date -u +%Y%m%dT%H%M%SZ)"
          git pull --rebase origin main || true
          git push
