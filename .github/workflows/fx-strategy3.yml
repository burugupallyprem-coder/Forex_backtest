name: fx-strategy3

on:
  workflow_dispatch:   # manual only - research experiment (overlap breakout)

permissions:
  contents: write

concurrency:
  group: fx-strategy3
  cancel-in-progress: false

jobs:
  strategy3:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Integrity check (guards against corrupted files)
        run: python -m compileall -q src tests

      - name: Unit tests
        run: |
          python tests/test_prevday_fx.py
          python tests/test_strategy3.py

      - name: Run overlap opening-range breakout research
        env:
          OANDA_API_KEY: ${{ secrets.OANDA_API_KEY }}
          OANDA_ENV: practice
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        run: python -m src.backtest.strategy3_overlap

      - name: Commit reports
        if: always()
        run: |
          git config user.name "fx-research-bot"
          git config user.email "fx-research-bot@users.noreply.github.com"
          git add reports/
          git diff --cached --quiet || git commit -m "fx strategy3 overlap research $(date -u +%Y%m%dT%H%M%SZ)"
          git pull --rebase origin main || true
          git push
